This directory contains LanguageWare part of speech tagger config files.  
The Current verison of the LanguageWare part of speech tagger requires
these files to reside in the directory ${UIMA_DATAPATH}/Tagger/<language>,
Where <language> is the appropriate two-letter language code.